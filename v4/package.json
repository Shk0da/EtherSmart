{
  "name": "ethersmart-v4",
  "version": "1.0.0",
  "private": true,
  "description": "HonestFlashArbV4 — multi-leg V2/V3/Curve/Balancer flash arbitrage",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:mock": "hardhat test test/honestFlashArbV4.mock.test.js",
    "deploy": "hardhat run scripts/deploy.js --network mainnet",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
