{
  "name": "ethersmart-v3",
  "version": "1.0.0",
  "private": true,
  "description": "HonestFlashArbV3 — mixed V2/V3 flash loan arbitrage",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:mock": "hardhat test test/honestFlashArbV3.mock.test.js",
    "deploy": "hardhat run scripts/deploy.js --network mainnet",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
